name: Build Kernel with SukiSU

on:
  workflow_dispatch:
    inputs:
      select_device:
        description: 'Select device:'
        required: true
        type: choice
        options:
          - 'merlin'

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout kernel source
        uses: actions/checkout@v3
        with:
          path: kernel

      - name: Integrate SukiSU-Ultra
        run: |
          cd kernel
          curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s nongki
          
          # Verify SukiSU integration
          if [ -f "drivers/kernel_pm/kpm.c" ]; then
            echo "✅ SukiSU-Ultra integrated successfully"
          else
            echo "❌ SukiSU-Ultra integration failed"
            exit 1
          fi

      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libncurses-dev bison flex \
          libssl-dev libelf-dev bc git ccache python3

      - name: Build Kernel
        uses: dabao1955/kernel_build_action@main
        with:
          kernel-dir: kernel
          config: exynos850_defconfig
          arch: arm64
          android-version: 12
          # ✅ Fixed: Use GCC toolchain instead of Clang for compatibility
          aosp-clang: false
          aosp-gcc: true
          aosp-gcc-version: "arm64-12"
          anykernel3: true
          # Optional: Add ccache for faster builds
          use-ccache: true

      - name: Upload Kernel Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Compiled-Kernel-Exynos850
          path: |
            kernel/out/AnyKernel3/*.zip
            kernel/out/arch/arm64/boot/Image
            kernel/out/.config
          retention-days: 7

      - name: Display Build Info
        if: success()
        run: |
          echo "✅ Build completed successfully!"
          echo "📦 Artifact name: Compiled-Kernel-Exynos850"
          echo "📁 Find it in: GitHub Actions → Artifacts section"
          echo "⚡ Kernel includes: SukiSU-Ultra integration"
